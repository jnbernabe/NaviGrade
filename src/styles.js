// styles.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#3498db", // A modern blue color
    },
    secondary: {
      main: "#e74c3c", // A modern red color
    },
  },
});

export default theme;
